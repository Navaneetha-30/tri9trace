# Required only for the /generate route (LLM) and for Mongo-backed generation storage.
# Ingestion and browse routes work with zero external services configured.
GROQ_API_KEY=your_groq_key_here
# MongoDB Atlas connection string. Leave blank to use the local JSON-file store.
MONGODB_URI=